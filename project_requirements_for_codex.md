# 校园风险文本识别与可视化分析平台（Demo v1）需求整理

## 1. 项目定位

本项目第一阶段目标不是做完整的“舆情管理/心理健康平台”，而是在 **1 个月内** 做出一个 **可演示、可交互、可落地的 Web Demo**。

Demo 核心定位：

> 面向校园场景的风险文本识别与可视化分析原型系统

系统面向的文本风险主要包括两类：
- **心理风险**：极端消极、自伤倾向、强烈绝望表达等
- **舆情风险**：对学校的强烈负面抱怨、投诉、冲突性言论、可能引发校园舆情的问题表达等

同时系统保留对：
- **一般负面** 文本
- **正常文本**

的识别能力。

---

## 2. 第一阶段目标（必须实现）

在一个月内完成一个 **“风险文本识别 + 可视化展示 + 少量真实数据接入”** 的小型 Web Demo。

### Demo 必须具备的能力

1. **单条文本分析**
   - 用户手动输入一段文本
   - 系统返回：
     - 类别
     - 等级
     - 风险分数
     - 命中关键词
     - 规则说明
     - 大模型解释

2. **批量文本分析**
   - 支持上传文件（CSV / TXT / Excel 均可，后续开发时再定具体格式）
   - 系统批量输出分析结果
   - 支持结果列表展示、搜索、筛选

3. **可视化展示**
   - 风险类别分布
   - 风险等级分布
   - 高频关键词 / 词云
   - 高风险文本列表
   - 点击查看详情

4. **少量真实互联网公开数据接入**
   - 能现场展示“从互联网上找到少量公开文本并分析”的过程
   - 不要求大规模抓取，不要求复杂反爬对抗
   - 该模块只用于体现工程落地价值

---

## 3. 第一阶段不做的事情（明确边界）

以下内容 **不作为 Demo v1 的目标**：

- 不做完整校园舆情系统
- 不做心理诊断或医学判断
- 不做针对具体个人的监控或画像
- 不做复杂权限系统 / 用户系统
- 不做多平台大规模实时爬虫
- 不做复杂流式监控
- 不做自训练大模型
- 不追求学术最优精度
- 不把项目重心放在反爬或数据采集工程上

项目第一阶段只要求：

> 跑通“文本输入 → 风险识别 → 结果解释 → 可视化展示”这条闭环链路。

---

## 4. 风险识别标签体系（已确定）

### 4.1 一级类别

系统输出的一级类别固定为 4 类：

1. **心理风险**
2. **舆情风险**
3. **一般负面**
4. **正常文本**

### 4.2 二级等级

对于风险相关类别，等级划分为 3 档：

- **低**
- **中**
- **高**

说明：
- “正常文本”在界面展示时可以直接显示为“正常”，不强调风险等级
- 其余三类根据分数/规则/模型结果输出低中高等级

### 4.3 每条文本的理想输出结构

每条文本分析结果至少应包含：

- `category`：一级类别
- `level`：等级（低 / 中 / 高）
- `score`：风险分数
- `keywords`：命中关键词
- `rule_reason`：规则说明
- `llm_explanation`：大模型自然语言解释

---

## 5. 风险识别技术路线（已确定）

项目采用三层推理结构：

### 5.1 规则层

规则层用于：
- 命中高风险词
- 命中特定表达模式
- 提供基础可控分数
- 做高风险兜底

规则层的价值：
- 易实现
- 可解释
- 可控
- 适合 Demo 阶段快速落地

### 5.2 小模型层

本地轻量模型用于：
- 辅助文本分类
- 提升规则方法的泛化能力
- 与规则结果融合形成更稳定的主判定

原则：
- 不以“自己训练复杂模型”为第一阶段重点
- 优先选择可本地运行的轻量中文文本分类能力

### 5.3 大模型层

本地大模型用于：
- 生成自然语言解释
- 对高风险样本做补充说明
- 增强页面展示效果与可解释性

原则：
- **大模型不作为唯一裁决器**
- 第一阶段中，大模型主要承担“解释层”职责
- 批量分析时不必对每条文本都跑大模型，可优先针对高风险文本或详情查看时调用

### 5.4 总体判定逻辑

第一阶段推荐逻辑：

> **规则层 + 小模型层 = 主判定**
>
> **大模型层 = 解释增强**

---

## 6. 数据策略（已确定）

### 6.1 第一阶段数据来源

采用：

> **人工整理样本 + 少量爬虫/公开数据采集**

### 6.2 数据策略说明

- **人工样本**：用于保证 Demo 可以快速跑通、便于调试和标注
- **少量公开数据**：用于体现系统具备真实场景输入能力
- 不将项目成败绑定在大规模爬虫上

### 6.3 数据内容建议

人工样本至少覆盖：
- 正常文本
- 一般负面文本
- 舆情风险文本
- 心理风险文本

其中高风险文本需要覆盖典型触发样例，便于测试规则与模型解释。

---

## 7. Web Demo 功能需求（已确定）

用户明确要求第一版尽量包含以下功能：

1. **文本列表 + 风险标签 + 风险分数**
2. **高风险文本详情页 / 详情弹窗**
3. **统计图表**
4. **关键词 / 词云**
5. **搜索 / 筛选功能**
6. **手动输入一条文本并实时分析**
7. **批量导入文本文件后统一分析**
8. **最好能演示“现场从互联网上拿一小段公开数据并分析”**

### 7.1 推荐页面组织方式

用户倾向于做 **大屏驾驶舱风格**，且希望“唬人一点”。

因此推荐第一阶段采用：

> **单页大屏 + 若干弹窗 / 抽屉**

而不是复杂多页面后台系统。

### 7.2 大屏页面建议包含的模块

- 顶部：系统标题、时间、总体统计卡片
- 左侧：风险类别分布、风险等级分布、关键词词云
- 中部：核心风险文本列表 / 滚动列表
- 右侧：
  - 单条文本分析入口
  - 批量导入入口
  - 真实数据样例抓取入口
- 弹窗 / 抽屉：高风险文本详情

---

## 8. 技术栈倾向（已确定）

### 前端
- **Vue 3**
- **Vite**
- **Pinia**
- **Vue Router**
- **Element Plus**
- **ECharts**
- 词云组件（如 `echarts-wordcloud`）

### 后端
- **FastAPI**

### 模型服务
- 规则引擎：Python 实现
- 小模型：本地轻量分类模型
- 大模型：本地部署后调用（如本地模型服务）

---

## 9. 部署与开发现实约束

### 9.1 当前团队实际情况

- 技术开发基本由 1 人负责
- 另外 2 人主要承担：
  - 数据整理与标注
  - 风险词表整理
  - 文书/PPT/汇报材料
  - 测试与样例准备

### 9.2 由此得出的开发原则

第一阶段方案必须满足：

- **单人可控开发**
- **链路尽量短**
- **界面效果好，但实现不能过重**
- **先保证完整闭环，再考虑扩展性**

### 9.3 开发重点排序

对 Demo 成功最关键的是：

1. 页面观感足够像“风险监测平台”
2. 后端能稳定返回分析结果
3. 结果解释清楚、可展示
4. 具备少量真实数据输入能力

而不是：

- 高并发
- 极高精度
- 超大规模数据源接入
- 复杂算法创新

---

## 10. 推荐的接口输出规范（供后续开发参考）

### 10.1 单条分析接口

**输入示例：**

```json
{
  "text": "最近真的要崩溃了，不想继续了",
  "mode": "hybrid"
}
```

**输出示例：**

```json
{
  "category": "心理风险",
  "level": "高",
  "score": 0.91,
  "keywords": ["崩溃", "不想继续"],
  "rule_reason": "命中极端消极表达词，并伴随明显绝望倾向短语",
  "llm_explanation": "文本包含强烈消极和绝望表达，建议人工重点关注。"
}
```

### 10.2 批量分析接口

批量分析结果建议返回数组，每条记录包含：

- 原始文本
- 一级类别
- 风险等级
- 风险分数
- 命中关键词
- 是否需要重点关注

---

## 11. 演示价值点（答辩时可重点强调）

Demo 的亮点不在于“做得多大”，而在于“小而完整”。

### 可以强调的价值

1. **有清晰应用场景**
   - 面向校园风险文本分析

2. **有完整链路**
   - 文本输入 / 真实样例接入 → 风险识别 → 解释 → 可视化展示

3. **有可解释能力**
   - 不只输出标签，还能输出关键词、规则说明和大模型解释

4. **有真实工程感**
   - 支持导入数据
   - 支持现场抓取少量公开样例并分析
   - 支持搜索、筛选、详情查看

5. **技术路线可扩展**
   - 后续可增加更好的分类模型、更多数据源、更复杂的预警逻辑

---

## 12. 当前阶段的最终定义（高度浓缩版）

### 一句话定义

> 在 1 个月内完成一个面向校园场景的风险文本识别与可视化 Web Demo，支持单条分析、批量分析、少量公开数据接入，并输出类别、等级、分数、关键词、规则说明和大模型解释。

### 最核心的产品闭环

> **输入文本 → 风险识别 → 风险分级 → 解释说明 → 可视化展示**

### 第一阶段关键词

- 校园场景
- 风险文本识别
- 大屏驾驶舱
- 规则 + 小模型 + 本地大模型解释
- 单人可控开发
- 小而完整的工程 Demo

---

## 13. 给 Codex 的实现提示（直接面向开发）

实现时应优先保证以下事项：

1. 先实现一个可运行的完整链路，不追求复杂度
2. 前端优先保证“大屏驾驶舱风格”的视觉效果
3. 后端优先保证分析结果结构稳定
4. 规则层必须先可用，小模型和大模型在此基础上增强
5. 大模型默认只承担解释层职责
6. 批量分析与单条分析都必须可演示
7. 真实互联网数据接入只需小规模、受控、可展示
8. 所有设计都应服从“一个月内、单人技术开发可完成”的约束

